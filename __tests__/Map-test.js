import React from 'react';
import renderer from 'react-test-renderer';

test('renders map page correctly', () => {

    //const tree = renderer.create(<Map />).toJSON()
    //expect(tree).toMatchSnapshot();
});